# NEX-OS Scope — In / Out / Later (Garrett Scope Plane)

**In (v1 Ship):**
- 7 Apps: Home (nexus card), Projects (Nautilus gallery, before/after), About (bio stack), Experience (VoltEdge timeline), Contact (emailjs), Terminal (real commands), Settings (wallpaper/accent, you provide /walls/*)
- Shell: MenuBar File/Edit/View, TopBar clock, Dock 5 pinned, WindowManager drag/resize + B-loop limit 5, Search Cmd+K, BIOS preloader
- System: MCP /mcp endpoint exposing projects JSON, Glass Regular nav only

**Out (Not in v1):**
- Media paint/dashboard/games, music player default (webamp fallback kept but not primary), themes beyond accent, filesystem write

**Later (v1.1):**
- MakingOf app (RI transparency), Live telemetry Canvas (MQTT), Liquid Glass lensing Tier2 Chromium SVG
